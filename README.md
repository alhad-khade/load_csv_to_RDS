# load_csv_to_RDS
This repo describes the workflow where a CSV file (from external or internal producer/source) when received in an AWS S3 bucket can trigger an AWS Lambda which loads the data to an AWS RDS MySQL database.
